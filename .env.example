# ┌──────────────────────────────────────────────────────────────┐
# │  VPN GlobalProtect — Variables de Entorno                    │
# │  COPIAR este archivo como .env y NUNCA commitear .env        │
# └──────────────────────────────────────────────────────────────┘

# OBLIGATORIO — Generar con:
# python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=REEMPLAZAR_CON_CLAVE_GENERADA

# Flask debug (solo activar en dev local, NUNCA en PRD)
FLASK_DEBUG=0

# ── SMTP (Gmail) ────────────────────────────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=rodrigostuardos@gmail.com
# App Password de Google (no contraseña real de Gmail)
SMTP_PASS=
SMTP_FROM=rodrigostuardos@gmail.com
